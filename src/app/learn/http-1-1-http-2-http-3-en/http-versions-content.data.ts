import { ChapterBlock } from '../fundamentos-internet-redes-apis/fundamentos-content.data';

// Full translation of the supplied PDF; only headers, footers, and physical page breaks were removed.
export const HTTP_VERSIONS_EN_CHAPTER_BLOCKS: ChapterBlock[] = [
  {
    "kind": "heading",
    "level": 2,
    "text": "Chapter overview",
    "id": "chapter-overview"
  },
  {
    "kind": "paragraph",
    "text": "HTTP is often presented through short GET and POST examples, but this view is insufficient for those operating enterprise gateways. In production, HTTP is a protocol with its own semantics, strict delimitation rules, negotiation mechanisms, cache, conditions, intermediaries and different transport mappings. A call that appears to be a single request can be received in HTTP/3 at the edge, converted to HTTP/2 up to the gateway, and forwarded in HTTP/1.1 to the backend. Each hop has its own connection, state, timeout, field compression and operational risk."
  },
  {
    "kind": "paragraph",
    "text": "The HTTP/1.1, HTTP/2, and HTTP/3 versions do not represent three different APIs. They share methods, status codes, fields, and representation concepts defined by HTTP semantics, but they encode and transport these elements in different ways. HTTP/1.1 uses a textual syntax over a TCP stream; HTTP/2 introduces binary frames and multiplexed streams over a connection; HTTP/3 takes HTTP semantics to QUIC, which works over UDP and integrates TLS 1.3 into the transport."
  },
  {
    "kind": "paragraph",
    "text": "For an API Gateway engineer, the distinction between semantics and wire versioning is central. A 405 error is not a consequence of TCP or QUIC; it indicates that the method is not allowed by the resource. An HTTP no response timeout points to another layer. A 502 may have been generated by the gateway after a failed connection to the upstream. Intermittent behavior in HTTP/2 may involve stream limits, flow window, drainage or an inappropriate translation to HTTP/1.1 in the next hop."
  },
  {
    "kind": "paragraph",
    "text": "This chapter builds a complete mental model: first establishes the common semantics, then delves into the format and connections of HTTP/1.1, then explains the frame and stream architecture of HTTP/2, and finally introduces QUIC and HTTP/3. The goal is not to memorize lists, but to understand which properties remain stable, which change between versions and what evidence should be collected at each point in an enterprise architecture."
  },
  {
    "kind": "subhead",
    "text": "Central principle"
  },
  {
    "kind": "paragraph",
    "text": "HTTP semantics pertain to the operation; the HTTP version and connection belong to each hop. A proxy finishes a message, applies rules, and creates a new message for the next destination."
  },
  {
    "kind": "heading",
    "level": 3,
    "text": "Learning objectives"
  },
  {
    "kind": "list",
    "ordered": false,
    "items": [
      "Explain the semantic architecture of HTTP and separate meaning, representation, and transport mapping.",
      "Interpret method, request target, authority, fields, content, trailers and status codes.",
      "Distinguish security, idempotency and cacheability of the main methods.",
      "Design caching and revalidation using Cache-Control, Age, ETag, Last-Modified and conditional requests.",
      "Interpret HTTP/1.1 syntax, including CRLF, Content-Length, Transfer-Encoding, and contentless messages.",
      "Understand persistent connections, pipelining, keep-alive, timeouts and head-of-line blocking.",
      "Recognize risks of divergent parsing, request smuggling, response splitting and hop-by-hop fields.",
      "Explain frames, streams, multiplexing, flow control, GOAWAY and HPACK in HTTP/2.",
      "Understand ALPN, h2, h2c, connection coalescing and concurrency limits.",
      "Explain QUIC, TLS 1.3 integration, Connection IDs, migration, loss, and independent streams.",
      "Understand HTTP/3 frames, control streams, QPACK, Alt-Svc, and HTTPS/SVCB discovery.",
      "Compare HTTP/1.1, HTTP/2, and HTTP/3 on latency, observability, security, and operation.",
      "Apply the concepts to Axway API Gateway, Azure API Management and architectures with multiple proxies.",
      "Diagnose failures through traces, headers, logs, connection metrics and traffic captures."
    ]
  },
  {
    "kind": "heading",
    "level": 3,
    "text": "Chapter structure"
  },
  {
    "kind": "table",
    "caption": "Table 1 - Conceptual organization of the chapter.",
    "headers": [
      "Block",
      "Content",
      "Guiding question"
    ],
    "rows": [
      [
        "A",
        "HTTP Semantics",
        "What does the message mean regardless of version?"
      ],
      [
        "B",
        "HTTP/1.1",
        "How are textual messages delimited within a TCP stream?"
      ],
      [
        "C",
        "HTTP/2",
        "How do frames and streams efficiently share a connection?"
      ],
      [
        "D",
        "HTTP/3 and QUIC",
        "How to reduce blockages and make connection, security and mobility part of transportation?"
      ],
      [
        "And",
        "Gateways and operation",
        "How do different versions coexist and how to locate flaws in each jump?"
      ]
    ]
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "HTTP: application protocol and semantic architecture",
    "id": "http-application-protocol-and-semantic-architecture"
  },
  {
    "kind": "paragraph",
    "text": "RFC 9110 defines HTTP as a stateless application protocol for distributed systems and establishes concepts common to all versions. “Stateless” does not mean that applications cannot maintain session or business state. It means that the protocol does not require the server to preserve, between independent requests, the context necessary to interpret the next message. State can be represented in resources, databases, cookies, tokens, caches or sessions, but each request must carry enough information to be processed according to the contract."
  },
  {
    "kind": "paragraph",
    "text": "The main semantic unit is a request or response message. A request expresses a method applied to a target, accompanied by fields and, optionally, content. A response communicates the result through a status code, fields, and possible content. HTTP does not determine that the content is JSON nor that the API is REST. The protocol can transport HTML, images, XML, Protobuf, files, events and other formats registered or agreed between the parties."
  },
  {
    "kind": "paragraph",
    "text": "Semantics also defines properties that guide intermediaries. Caches need to know when a response can be reused; proxies need to differentiate fields destined for the next hop from end-to-end metadata; customers need to decide when an operation can be repeated after failure; gateways need to preserve authority, method and content without creating ambiguity. These properties are more important for interoperability than the textual appearance seen in an HTTP/1.1 example."
  },
  {
    "kind": "figure",
    "src": "/assets/learn/http-versions/en/figure-01-shared-semantics.svg",
    "alt": "Common HTTP semantics mapped differently by HTTP/1.1, HTTP/2, and HTTP/3",
    "caption": "Figure 1 - The semantics are shared; each version defines a different mapping for the network."
  },
  {
    "kind": "subhead",
    "text": "Reading for gateways"
  },
  {
    "kind": "paragraph",
    "text": "When a gateway receives HTTP/2 and calls a backend on HTTP/1.1, it does not “forward frames”. It decodes a message, executes policies, and generates another message compatible with the next protocol."
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Evolution: from minimal messages to multiplexed transport",
    "id": "evolution-from-minimal-messages-to-multiplexed-transport"
  },
  {
    "kind": "paragraph",
    "text": "HTTP/0.9 had an extremely simple form: a GET request followed by the path, and a response composed by the document. There were no status codes, fields, type negotiation, or persistent connection. This simplicity suited early Web documents, but did not offer suitable mechanisms for complex distributed applications. HTTP/1.0 introduced initial line versions, status codes, fields, and media types, allowing you to represent metadata and different content."
  },
  {
    "kind": "paragraph",
    "text": "HTTP/1.1 consolidated persistent connections by default, the Host field, more complete caching, conditional requests, chunked transfers and framing rules. The protocol now supports multiple sites on the same IP address and reduces the cost of opening a connection per object. Still, the reliance on an ordered TCP stream and sequential format has created limits for pages and APIs with many concurrent operations."
  },
  {
    "kind": "paragraph",
    "text": "HTTP/2 preserved the semantics and replaced the textual message format with binary frames associated with streams. This allowed us to interleave multiple exchanges on the same connection and compress repetitive fields with HPACK. HTTP/3 maintained the streams and frames model, but replaced TCP with QUIC. Thus, loss in one stream does not need to prevent the delivery of data available from other streams, and the cryptographic handshake becomes part of the transport establishment."
  },
  {
    "kind": "figure",
    "src": "/assets/learn/http-versions/en/figure-02-http-evolution.svg",
    "alt": "Timeline of evolution from HTTP/0.9 to HTTP/3",
    "caption": "Figure 2 - Historical evolution of the HTTP protocol."
  },
  {
    "kind": "subhead",
    "text": "Semantic compatibility"
  },
  {
    "kind": "paragraph",
    "text": "A GET method is still GET in HTTP/1.1, HTTP/2, and HTTP/3. What changes is how method, target, fields and content are represented and transported."
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Transaction model: request, response and self-describing messages",
    "id": "transaction-model-request-response-and-self-describing-messages"
  },
  {
    "kind": "paragraph",
    "text": "An HTTP transaction begins when a client sends a request to an origin or intermediary server. The request contains control information and metadata that allows you to determine the resource, operation and expected form of response. The server produces one or more informational responses and a final response. In HTTP/1.1, elements appear as lines and fields; in HTTP/2 and HTTP/3, they are converted into pseudo-fields and frames."
  },
  {
    "kind": "paragraph",
    "text": "The content of a message is conceptually different from its representation on the wire. Fields such as Content-Type and Content-Encoding describe the representation. Framing tells you how to locate content within the connection. In HTTP/1.1, Content-Length or chunked can delimit the body; In HTTP/2 and HTTP/3, content is loaded in DATA frames and the end of the stream indicates completion. Confusing representation metadata with framing produces interoperability bugs and vulnerabilities."
  },
  {
    "kind": "paragraph",
    "text": "Trailers are fields sent after the content. They can carry calculated metadata at the end of the transmission, such as integrity or status of protocols built on HTTP. Not all intermediaries preserve trailers, and the contract needs to consider this possibility. On gateways, turning streaming into full buffering can change latency, memory consumed, and the ability to deliver trailers to the client."
  },
  {
    "kind": "figure",
    "src": "/assets/learn/http-versions/en/figure-03-message-anatomy.svg",
    "alt": "Conceptual anatomy of an HTTP request and response",
    "caption": "Figure 3 - Conceptual anatomy of request and response."
  },
  {
    "kind": "code",
    "text": "POST /v1/payments HTTP/1.1\nHost: api.example.com\nContent-Type: application/json\nAccept: application/json\nContent-Length: 42\n{\"amount\":100.00,\"currency\":\"BRL\"}"
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Identifiers, URI, authority and target of the request",
    "id": "identifiers-uri-authority-and-target-of-the-request"
  },
  {
    "kind": "paragraph",
    "text": "A URI identifies a resource or reference used in the interaction. In common API usage, the URI includes scheme, authority, path, and query. At `https://api.example.com:443/v1/accounts/123? view=summary`, the scheme is HTTPS, the authority contains host and port, the path identifies a logical hierarchy and the query adds parameters. The fragment, when present in a URI, is processed by the client and is not sent in the HTTP request."
  },
  {
    "kind": "paragraph",
    "text": "HTTP/1.1 uses different forms of request-target depending on the method and type of intermediary. The origin-form sends path and query; absolute-form is typical in forward proxies; authority-form is used by CONNECT; and the asterisk-form appears in OPTIONS `*`. The Host field is mandatory in HTTP/1.1 and informs the intended authority. In HTTP/2 and HTTP/3, pseudo-fields such as `:scheme`, `:authority`, `:path` and `:method` represent these components."
  },
  {
    "kind": "paragraph",
    "text": "Gateways must treat authority carefully because it participates in routing, certificate selection, policies, and protection against Host header attacks. A proxy can receive a public address and call a different internal host; This does not authorize indiscriminately substituting the meaning of the original authority. On some architectures, the backend needs to know the external host via `Forwarded` or equivalent fields, but these fields should only be trusted when entered by controlled intermediaries."
  },
  {
    "kind": "table",
    "caption": "Table 2 - Common components of an HTTP URI.",
    "headers": [
      "Component",
      "Example",
      "Technical use"
    ],
    "rows": [
      [
        "scheme",
        "https",
        "Defines security schema and expectations."
      ],
      [
        "Authority",
        "api.example.com:443",
        "Identifies host and logical port of the source."
      ],
      [
        "Path",
        "/v1/accounts/123",
        "Selects resource or route."
      ],
      [
        "Query",
        "view=summary",
        "Non-hierarchical parameters."
      ],
      [
        "Fragment",
        "#section",
        "It is not sent to the server."
      ]
    ]
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Methods: intent, safety, and idempotency",
    "id": "methods-intent-safety-and-idempotency"
  },
  {
    "kind": "paragraph",
    "text": "The method expresses the intent of the request. GET requests the transfer of a representation; HEAD requests the same metadata without response content; POST asks the resource to process the content according to its semantics; PUT replaces or creates state in the target URI; DELETE requests the removal of the current association; OPTIONS describes communication options; CONNECT establishes a tunnel; TRACE runs a loopback diagnostic and is typically disabled in corporate environments."
  },
  {
    "kind": "paragraph",
    "text": "A secure method is defined as essentially read-only: the client does not request a change to the server's state. This doesn't prevent logs, metrics, or operational billing, but it does mean that the intended effect is not a business change. Idempotence means that repeating the same intention multiple times should produce the same desired effect as a single execution. PUT and DELETE are semantically idempotent, although observable responses and side effects may vary."
  },
  {
    "kind": "paragraph",
    "text": "The distinction is decisive for retries. A gateway can repeat a GET request after connection failure with less semantic risk. Retrying payment POST may duplicate the operation if the backend processed the content before the response was lost. Financial APIs often introduce an application idempotence key, persisting the result associated with a unique key. This strategy complements, but does not change, the semantics of the HTTP method."
  },
  {
    "kind": "figure",
    "src": "/assets/learn/http-versions/en/figure-04-method-properties.svg",
    "alt": "Security and idempotence comparison of main HTTP methods",
    "caption": "Figure 4 - Semantic properties of the main methods."
  },
  {
    "kind": "subhead",
    "text": "Retry is not just a technical decision"
  },
  {
    "kind": "paragraph",
    "text": "Before configuring retries on the gateway, classify the operation, determine the point at which the upstream may have taken effect, and define how duplicates will be detected."
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Status codes and response authorship",
    "id": "status-codes-and-response-authorship"
  },
  {
    "kind": "paragraph",
    "text": "The status code describes the result of the attempt to interpret and satisfy the request. 1xx responses are informative; 2xx indicate success; 3xx advise redirection or use of another representation; 4xx indicate that the request cannot be met under the conditions presented; 5xx report server or intermediary failure. The class is relevant, but the specific code and associated fields provide the complete semantics."
  },
  {
    "kind": "paragraph",
    "text": "In architectures with proxies, the final code can be created by any intermediary. A 401 can come from a gateway authentication policy without the backend being called. A 429 can represent the edge's rate limit. A 502 typically indicates that a gateway did not get a valid response from upstream; a 504 represents timeout acting as a gateway. Logs must record authorship, policy stage, and upstream status separately."
  },
  {
    "kind": "paragraph",
    "text": "HTTP/1.1 reason phrases are informational and should not control logic. HTTP/2 and HTTP/3 carry the code in `:status` and do not carry reason phrase. Customers should make decisions based on the number and fields, not text like “OK” or “Unauthorized”. In APIs, error responses can add an issue format, correlation, and details, but the body does not replace status semantics."
  },
  {
    "kind": "table",
    "caption": "Table 3 - Frequent codes in corporate APIs.",
    "headers": [
      "Code",
      "Operational meaning",
      "Point of attention in gateways"
    ],
    "rows": [
      [
        "200",
        "Operation completed with representation.",
        "It can be cached according to fields and method."
      ],
      [
        "201",
        "Resource created.",
        "Location can identify the new resource."
      ],
      [
        "204",
        "Success without content.",
        "Do not invent a body during transformation."
      ],
      [
        "304",
        "Stored representation remains valid.",
        "It's not a normal body response."
      ],
      [
        "400",
        "Invalid request.",
        "It can come from parsing, validation or contract."
      ],
      [
        "401",
        "Missing or invalid credentials.",
        "WWW-Authenticate describes the challenge."
      ],
      [
        "403",
        "Entity understood but not authorized.",
        "Not to be confused with authentication failure."
      ],
      [
        "404",
        "Resource not found or hidden.",
        "It could come from gateway routing."
      ],
      [
        "409",
        "Conflict with current state.",
        "Useful in competition and idempotence."
      ],
      [
        "413",
        "Content too big.",
        "Limit may exist in each intermediary."
      ],
      [
        "429",
        "Limit exceeded.",
        "Retry-After can guide retry."
      ],
      [
        "502",
        "Invalid response from upstream.",
        "Investigate connection and parsing between gateway and backend."
      ],
      [
        "503",
        "Service unavailable.",
        "Empty pool, maintenance or overload."
      ],
      [
        "504",
        "Gateway Timeout.",
        "Find out which hop expired first."
      ]
    ]
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "HTTP fields: metadata, scope and normalization",
    "id": "http-fields-metadata-scope-and-normalization"
  },
  {
    "kind": "paragraph",
    "text": "HTTP fields are extensible name-value pairs. They carry information about control, representation, caching, authentication, negotiation and intermediaries. Names are case-insensitive, although values ​​may have specific rules. A field can allow a single occurrence, multiple occurrences, or a composable list. Implementations should not concatenate fields indiscriminately, especially `Set-Cookie`, whose semantics require specific treatment."
  },
  {
    "kind": "paragraph",
    "text": "RFC 9110 distinguishes end-to-end fields from connection-related information. HTTP/1.1 uses the Connection field to declare field names that apply only to the next hop. Intermediaries must remove these fields before forwarding. HTTP/2 and HTTP/3 prohibit several connection-specific fields, as streams and frames replace mechanisms like `Transfer-Encoding: chunked`. Replicating HTTP/1.1 headers without understanding their scope can lead to protocol failures."
  },
  {
    "kind": "paragraph",
    "text": "Normalization is necessary but dangerous. Gateways can change capitalization, order, combine lines, remove optional spaces, or reconstruct messages. Applications should not depend on the order of fields. On the other hand, HTTP signatures and authentication schemes can define explicit canonicalization. A gateway policy that modifies signed values ​​must be executed before signing or preserve exactly the covered components."
  },
  {
    "kind": "table",
    "caption": "Table 4 - Relevant field categories for APIs.",
    "headers": [
      "Category",
      "Examples",
      "Function"
    ],
    "rows": [
      [
        "Routing and targeting",
        "Host, Forwarded",
        "Identify authority and path through intermediaries."
      ],
      [
        "Representation",
        "Content-Type, Content-Encoding, Content-Language",
        "Describe the transferred content."
      ],
      [
        "Negotiation",
        "Accept, Accept-Encoding, Accept-Language",
        "Express customer preferences."
      ],
      [
        "Cache",
        "Cache-Control, Age, ETag, Vary",
        "Control storage and reuse."
      ],
      [
        "Authentication",
        "Authorization, WWW-Authenticate",
        "Present credential or challenge."
      ],
      [
        "Condition",
        "If-Match, If-None-Match, If-Modified-Since",
        "Execute operation according to known state."
      ],
      [
        "Observability",
        "traceparent, tracestate, correlation",
        "Propagate context between services."
      ]
    ]
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Representations, media types and content negotiation",
    "id": "representations-media-types-and-content-negotiation"
  },
  {
    "kind": "paragraph",
    "text": "A resource is an abstraction; the representation is a sequence of bytes and metadata that describes a state of that resource. The same resource can be represented as JSON, XML, CSV, or image. `Content-Type` reports the media type of the sent representation, while `Accept` expresses which types the client considers acceptable. Sending JSON without `Content-Type: application/json` forces the receiver to infer or reject the content."
  },
  {
    "kind": "paragraph",
    "text": "Content negotiation can occur proactively, using Accept, Accept-Encoding and Accept-Language fields, or reactively, when the server offers alternatives. The Vary field informs which dimensions of the request influenced the response selection and is essential for caches. If a response varies by `Accept-Encoding`, a cache should not hand over gzipped bytes to a client that does not accept them."
  },
  {
    "kind": "paragraph",
    "text": "Content-Encoding describes transformations applied to the representation, such as gzip or br. It should not be confused with Transfer-Encoding, which in HTTP/1.1 participates in message framing. Gateways that decompress content for inspection need to decide whether to recompress, correct Content-Length, preserve ETag when semantically valid, and consider expansion limits to prevent decompression attacks."
  },
  {
    "kind": "code",
    "text": "GET /reports/2026 HTTP/1.1\nHost: api.example.com\nAccept: application/json, application/xml;q=0.8\nAccept-Encoding: br, gzip\nHTTP/1.1 200 OK\nContent-Type: application/json\nContent-Encoding: gzip\nVary: Accept, Accept-Encoding"
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "HTTP cache: freshness, reuse and invalidation",
    "id": "http-cache-freshness-reuse-and-invalidation"
  },
  {
    "kind": "paragraph",
    "text": "HTTP caching is not just a browser optimization. CDNs, reverse proxies, and gateways can store responses and serve requests without calling the backend. RFC 9111 defines when a response can be stored, considered fresh, reused, or revalidated. Cache-Control carries directives such as max-age, no-cache, no-store, private, public and must-revalidate. `no-cache` does not mean “do not store”; means that the response needs to be validated before reuse, except for specific rules."
  },
  {
    "kind": "paragraph",
    "text": "The age of a response considers the time since its generation and permanence in caches. The Age field informs estimated age. Shared caches must respect authorization and privacy policies. In banking APIs, storing custom responses without proper cache key can cause leakage between users. The key typically includes URI and dimensions indicated by Vary, but gateways can add specific rules depending on identity, tenant or product."
  },
  {
    "kind": "paragraph",
    "text": "Invalidating cache is difficult because copies can exist at multiple levels. Strategies include short TTL, URI versioning, explicit purging, and revalidation. The choice needs to balance consistency, cost and availability. An unavailable backend can be protected by stored responses, but serving old information in financial operations may be unacceptable. Cache policy is a domain decision, not just a performance one."
  },
  {
    "kind": "figure",
    "src": "/assets/learn/http-versions/en/figure-05-etag-revalidation.svg",
    "alt": "Conditional cache revalidation flow with ETag",
    "caption": "Figure 5 - Conditional revalidation with ETag."
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Conditional requests and concurrency control",
    "id": "conditional-requests-and-concurrency-control"
  },
  {
    "kind": "paragraph",
    "text": "Conditional requests allow you to perform an operation only if the resource state satisfies a condition. `If-None-Match` is used to revalidate cache or guarantee creation only when no current representation exists. `If-Match` requires that the current entity matches an ETag and is useful for avoiding lost updates. Date-based fields, such as If-Modified-Since and If-Unmodified-Since, have lower granularity and reliability than opaque validators."
  },
  {
    "kind": "paragraph",
    "text": "ETag is an identifier of the selected representation. A strong ETag indicates byte-to-byte equivalence suitable for operations such as ranges; a weak one indicates sufficient semantic equivalence for certain cache uses. It does not need to be a cryptographic hash or reveal an internal version. Gateways that transform content can invalidate the backend's ETag, because the representation delivered to the client is no longer the same."
  },
  {
    "kind": "paragraph",
    "text": "In optimistic control, the client reads a representation with ETag, changes it locally and sends PUT or PATCH with If-Match. If another actor modified the resource, the server responds 412 Precondition Failed. This avoids silently overwriting a concurrent update. The policy must be implemented in the component that controls the business state; a gateway can validate presence and format, but it does not know the current version of the resource on its own."
  },
  {
    "kind": "code",
    "text": "GET /accounts/123\n<- ETag: \"v17\"\nPATCH /accounts/123\nIf-Match: \"v17\"\nContent-Type: application/merge-patch+json\n{\"nickname\":\"Reserva\"}\n<- 204 No Content   or   412 Precondition Failed"
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "HTTP/1.1: textual syntax and byte stream",
    "id": "http-1-1-textual-syntax-and-byte-stream"
  },
  {
    "kind": "paragraph",
    "text": "HTTP/1.1 represents messages as a start line, field sequence, empty line, and optional content. Lines are delimited by CRLF. A request starts with method, request-target and version; a response starts with version and status. Although tools display messages in a readable form, the implementation needs to handle octets, limits, spaces and prohibited characters according to the specification. Excessive tolerance for invalid messages is a recurring source of inconsistency between intermediaries."
  },
  {
    "kind": "paragraph",
    "text": "The receiver needs to determine where each message ends in a persistent connection. TCP delivers a boundaryless message stream: a `recv()` can return part of a line, multiple lines, or data from consecutive messages. The HTTP parser accumulates bytes, recognizes the field section and applies framing rules. The connection cannot simply use “end of packet” as a boundary, as IP packets and TCP segments are inferior details that can be split or combined."
  },
  {
    "kind": "paragraph",
    "text": "In APIs, size limits for initial row, fields, and content exist across clients, WAFs, gateways, and servers. A field accepted by the edge may be rejected by the gateway; a payload allowed by the gateway may exceed the backend. Settings need to be coordinated. Errors 400, 413, 414 and 431 help to differentiate between malformed requests, large content, long URIs and excessive fields."
  },
  {
    "kind": "code",
    "text": "HTTP-message = start-line CRLF\n               *( field-line CRLF )\n               CRLF\n               [ message-body ]"
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Framing in HTTP/1.1: Content-Length, chunked and no body",
    "id": "framing-in-http-1-1-content-length-chunked-and-no-body"
  },
  {
    "kind": "paragraph",
    "text": "Content-Length reports the length of the content in octets. When valid, the receiver reads exactly that amount after the empty line. `Transfer-Encoding: chunked` encodes content into blocks, each preceded by hexadecimal size, and ending with chunk zero and optional trailers. Chunked allows you to transmit without knowing the total size in advance, but it is a hop-by-hop mechanism and does not appear in HTTP/2 or HTTP/3."
  },
  {
    "kind": "paragraph",
    "text": "Not every answer has a body. Responses to HEAD do not include content, although the fields describe what a GET would have returned. Codes 1xx, 204 and 304 have specific rules without content. Successful responses to CONNECT change the connection to tunnel mode. In other cases, closing the connection may limit the response, but this prevents reuse and is less robust."
  },
  {
    "kind": "paragraph",
    "text": "Multiple Content-Length with different values, improper combination of Content-Length and Transfer-Encoding or invalid chunked syntax need to be treated as an error. If a proxy and a backend choose different rules, they may disagree about where the request ends up. The result can range from connection corruption to request smuggling. The safe strategy is strict parsing and consistent normalization before forwarding."
  },
  {
    "kind": "figure",
    "src": "/assets/learn/http-versions/en/figure-06-http1-framing.svg",
    "alt": "Ways to delimit message content in HTTP/1.1",
    "caption": "Figure 6 - Two common ways to delimit content in HTTP/1.1."
  },
  {
    "kind": "table",
    "caption": "Table 5 - Length determination in HTTP/1.1.",
    "headers": [
      "Situation",
      "How length is determined",
      "Note"
    ],
    "rows": [
      [
        "Reply to HEAD",
        "No content",
        "Fields can describe an equivalent GET."
      ],
      [
        "1xx, 204, 304",
        "No content",
        "Framing should not invent bodysuits."
      ],
      [
        "CONNECT 2xx",
        "Tunnel after headers",
        "Following bytes are not ordinary HTTP messages."
      ],
      [
        "Chunked Final Transfer-Encoding",
        "Chunks down to size zero",
        "Trailers can follow chunk zero."
      ],
      [
        "Valid Content-Length",
        "Exact number of octets",
        "Conflicting values are an error."
      ],
      [
        "Response without explicit framing",
        "Until connection closes",
        "Not reusable and susceptible to truncation."
      ]
    ]
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Persistent connections, reuse, pooling and timeouts",
    "id": "persistent-connections-reuse-pooling-and-timeouts"
  },
  {
    "kind": "paragraph",
    "text": "HTTP/1.1 uses persistent connections by default. After a response, client and server can reuse TCP/TLS for new requests, reducing handshakes and latency. Reuse is only safe when the previous message has been completely delimited and consumed. If a client abandons a body without reading it, the connection may become misaligned for the next transaction."
  },
  {
    "kind": "paragraph",
    "text": "Gateways maintain pools of connections for backends. The pool separates the client connection from the upstream connection: hundreds of clients can share a pool of persistent connections, depending on competition and capacity. Relevant parameters include maximum per target, idle time, lifetime, connect timeout, read timeout, and validation before reuse. A load balancer can terminate idle connections before the gateway, causing resets by reusing apparently valid sockets."
  },
  {
    "kind": "paragraph",
    "text": "Timeouts must form a coherent budget. The customer has to wait longer than the edge; the edge, more than the gateway; the gateway, enough time for the backend, but less than the total consumer limit. If everyone uses the same value, components can expire simultaneously and produce retry storms. Logs need to distinguish connection time, sending, waiting for the first byte and reading the content."
  },
  {
    "kind": "table",
    "caption": "Table 6 - Connection timeouts and limits.",
    "headers": [
      "Parameter",
      "What limits",
      "Symptom when inappropriate"
    ],
    "rows": [
      [
        "Connect timeout",
        "TCP/TLS establishment upstream",
        "Fail quickly or wait too long before sending."
      ],
      [
        "Read/response timeout",
        "Waiting for response or data",
        "504 even when backend completes later."
      ],
      [
        "Idle timeout",
        "Time without traffic on reusable connection",
        "RST when reusing connection terminated by another hop."
      ],
      [
        "Max lifetime",
        "Total connection life",
        "Helps renew DNS and distribute connections."
      ],
      [
        "Pool size",
        "Concurrent connections by destination",
        "Local queue, latency or excess sockets."
      ],
      [
        "Total request timeout",
        "Operation budget",
        "It needs to include policies and all the jumps."
      ]
    ]
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Pipelining and head-of-line blocking in HTTP/1.1",
    "id": "pipelining-and-head-of-line-blocking-in-http-1-1"
  },
  {
    "kind": "paragraph",
    "text": "Pipelining allows you to send multiple requests over a connection without waiting for each response. However, responses must be issued in the same order. If the first request takes time, ready responses from subsequent requests are delayed. This blocking is known as head-of-line at the HTTP/1.1 level. The complexity of retries, intermediary servers, and inconsistent implementations has limited the practical adoption of pipelining."
  },
  {
    "kind": "paragraph",
    "text": "Customers have often worked around the problem by opening multiple parallel connections per source. This increases handshakes, port usage, queue congestion, and pressure on balancers. For APIs, multiple connections may distribute load differently than an HTTP/2 connection with many streams. Metrics based only on “active connections” fail to correctly represent competition when versions coexist."
  },
  {
    "kind": "paragraph",
    "text": "HTTP/2 was designed to multiplex independent requests without imposing global ordering of responses. Still, all frames travel over a TCP stream, so a segment loss can delay the delivery of later bytes from all streams. HTTP/3 moves stream isolation to QUIC transport."
  },
  {
    "kind": "figure",
    "src": "/assets/learn/http-versions/en/figure-07-http1-hol.svg",
    "alt": "Head-of-line blocking in HTTP/1.1 pipelining responses",
    "caption": "Figure 7 - Head-of-line in HTTP/1.1 pipelining."
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Intermediaries, hop-by-hop fields and transformation",
    "id": "intermediaries-hop-by-hop-fields-and-transformation"
  },
  {
    "kind": "paragraph",
    "text": "Proxy is an HTTP participant that receives one message and forwards another. It can select the next destination, apply authentication, transform content, cache, or record telemetry. A gateway acts as an intermediary with API-driven policies. The output message does not need to be byte-for-byte the same as the input, but it must preserve the defined semantics and meet security requirements."
  },
  {
    "kind": "paragraph",
    "text": "Hop-by-hop fields belong to a single connection and should not be automatically forwarded. In HTTP/1.1, Connection declares options applicable to the hop; TE has restricted use; Transfer-Encoding and Upgrade describe the current connection. HTTP/2 and HTTP/3 remove or replace these concepts. A gateway translates framing, and should not send `Transfer-Encoding: chunked` in an HTTP/2 stream."
  },
  {
    "kind": "paragraph",
    "text": "Transformations can change Content-Length, Content-Encoding, ETag, signature, cacheability and streaming. A policy that converts XML to JSON needs to define a new Content-Type and recalculate length. If the gateway buffers to validate schema, the first response may be slower and memory consumption may increase with payloads. The architecture must document which transformations are allowed and at which jump."
  },
  {
    "kind": "subhead",
    "text": "Operating rule"
  },
  {
    "kind": "paragraph",
    "text": "Capture the logical message before and after each relevant policy. Comparing only edge packets with backend logs ignores transformations introduced along the way."
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Divergent parsing and HTTP/1.1 message security",
    "id": "divergent-parsing-and-http-1-1-message-security"
  },
  {
    "kind": "paragraph",
    "text": "Request smuggling occurs when two participants interpret message boundaries differently. An attacker sends an ambiguous string; the first proxy considers a part as a body, while the second interprets remaining bytes as a new request. This can bypass controls, poison cache, mix responses between users or take unforeseen routes. Classical variations exploit conflicts between Content-Length and Transfer-Encoding, but the general problem is parsing disagreement."
  },
  {
    "kind": "paragraph",
    "text": "Response splitting explores the insertion of line delimiters into values that end up embedded in headers, creating additional fields or responses. Implementations need to reject disallowed CR and LF. Inconsistent normalization of spaces, names, and characters can also create discrepancies. The principle is to accept only valid syntax, reject ambiguities and avoid forwarding partially interpreted messages."
  },
  {
    "kind": "paragraph",
    "text": "HTTP/2 and HTTP/3 have binary framing, but can participate in attacks when an intermediary converts messages to HTTP/1.1 in an insecure way. An H2 request may contain metadata that, when serialized, creates ambiguities in the H1 backend. Therefore, security requires validation at the point of translation, not just at the edge. Recent specification updates also refine Upgrade requirements and anticipated data; Operators must keep track of errata and RFCs that update behavior."
  },
  {
    "kind": "figure",
    "src": "/assets/learn/http-versions/en/figure-08-request-smuggling.svg",
    "alt": "Request smuggling caused by divergent framing interpretations",
    "caption": "Figure 8 - Conceptual example of request smuggling through divergent framing."
  },
  {
    "kind": "subhead",
    "text": "Defense in depth"
  },
  {
    "kind": "paragraph",
    "text": "Use conforming parsers, remove ambiguous combinations, normalize once, keep frontend and backend updated, and explicitly test H2/H3 to H1 translations."
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "HTTP/2: binary framing layer",
    "id": "http-2-binary-framing-layer"
  },
  {
    "kind": "paragraph",
    "text": "HTTP/2 is an optimized expression of HTTP semantics. After the connection is established, participants exchange binary frames with a fixed header that informs length, type, flags and stream ID. HEADERS frames carry blocks of compressed fields; DATA carries content; SETTINGS negotiates parameters; WINDOW_UPDATE controls flow; RST_STREAM ends a stream; GOAWAY initiates coordinated termination of the connection."
  },
  {
    "kind": "paragraph",
    "text": "An HTTP message is mapped to a sequence of frames in a stream. The stream has an independent identifier and life cycle. An operation's request and response use the same stream. Frames from different streams can be interleaved, allowing concurrency without opening a TCP connection for each call. The receiver reconstructs each message using stream ID and flags such as END_HEADERS and END_STREAM."
  },
  {
    "kind": "paragraph",
    "text": "The protocol starts with a connection preface and exchange of SETTINGS. Parameters include field table size, maximum number of simultaneous streams, initial window size, and maximum frame size. SETTINGS needs to be confirmed. Settings are directional: what an endpoint advertises limits the peer's behavior when sending to it."
  },
  {
    "kind": "figure",
    "src": "/assets/learn/http-versions/en/figure-09-http2-streams.svg",
    "alt": "Frames from different streams interleaved over an HTTP/2 connection",
    "caption": "Figure 9 - Frames from multiple streams sharing an HTTP/2 connection."
  },
  {
    "kind": "table",
    "caption": "Table 7 - Important HTTP/2 frames.",
    "headers": [
      "Frame",
      "Scope",
      "Purpose"
    ],
    "rows": [
      [
        "DATE",
        "Stream",
        "Message content and possible END STREAM."
      ],
      [
        "HEADERS",
        "Stream",
        "Starter block or compressed trailers."
      ],
      [
        "SETTINGS",
        "Connection",
        "Directional parameters and capabilities."
      ],
      [
        "WINDOW UPDATE",
        "Connection or stream",
        "Increase flow control credit."
      ],
      [
        "RST STREAM",
        "Stream",
        "Abort specific operation."
      ],
      [
        "PING",
        "Connection",
        "Measure liveness/RTT without HTTP semantics."
      ],
      [
        "GOAWAY",
        "Connection",
        "Stop new streams and drain existing ones."
      ],
      [
        "PRIORITY UPDATE",
        "Stream",
        "Signal priority by current extensible template."
      ]
    ]
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Streams, states, and shutdown",
    "id": "streams-states-and-shutdown"
  },
  {
    "kind": "paragraph",
    "text": "Streams can be idle, open, half-closed, reserved or closed, depending on frames sent and received. END_STREAM closes the sender direction, allowing the other direction to continue. RST_STREAM immediately terminates the stream and reports an error code. Stream errors don't have to bring down the entire connection; Connection errors require GOAWAY and shutdown."
  },
  {
    "kind": "paragraph",
    "text": "GOAWAY contains the largest stream ID that may have been processed and an error code. The peer must not create new streams in the connection and can repeat stream operations above the limit, considering idempotence. In deployments and draining, gateways must issue GOAWAY and allow completion of accepted streams. Terminating TCP abruptly turns planned maintenance into indistinguishable network failures."
  },
  {
    "kind": "paragraph",
    "text": "The number of competing streams is limited by SETTINGS_MAX_CONCURRENT_STREAMS and local resources. A client may maintain a connection, but be blocked waiting for credit to open a new stream. Metrics should show active, pending, and rejected streams, not just connections. A single client with HTTP/2 can generate high competition for a connection."
  },
  {
    "kind": "table",
    "caption": "Table 8 - Lifecycle events in HTTP/2.",
    "headers": [
      "Event",
      "Effect",
      "Operational decision"
    ],
    "rows": [
      [
        "END STREAM in request",
        "Customer finished content.",
        "Gateway can initiate full processing or streaming."
      ],
      [
        "END STREAM in response",
        "Server finished message.",
        "Stream may close if both directions have ended."
      ],
      [
        "RST STREAM",
        "Cancels specific stream.",
        "Register launcher and code; business effect may already exist."
      ],
      [
        "GOAWAY NO ERROR",
        "Coordinated drainage.",
        "Open new connection for new streams."
      ],
      [
        "GOAWAY with error",
        "Connection failure.",
        "Evaluate retries by stream and idempotence."
      ],
      [
        "Stream limit",
        "New operations await.",
        "Increase connections or adjust competition cautiously."
      ]
    ]
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Multiplexing, flow control and memory pressure",
    "id": "multiplexing-flow-control-and-memory-pressure"
  },
  {
    "kind": "paragraph",
    "text": "Multiplexing allows frames to be interleaved, but does not eliminate the need for backpressure. HTTP/2 applies flow control to DATA at the connection and stream level. The receiver announces credit; as it consumes bytes, it sends WINDOW_UPDATE. If the application does not read a stream, its window may reset. If the connection window resets, all streams are prevented from sending DATA, even if some consumers are fast."
  },
  {
    "kind": "paragraph",
    "text": "Flow control is not the same as TCP congestion control. The first protects receiver and application buffers; the second adapts sending to the network capacity. Both act simultaneously. Increasing windows can improve throughput on high bandwidth-delay product paths, but increases potential memory. Gateways need to limit streams, frames, fields and content to prevent a few clients from monopolizing resources."
  },
  {
    "kind": "paragraph",
    "text": "The original HTTP/2 had a dependency and weighting mechanism for priorities that proved difficult to implement in an interoperable way. The current specification removes the old core model and allows for an extensible schema. Operators should not assume that client priority will be honored end-to-end, especially when there are proxies that reorder or convert protocols."
  },
  {
    "kind": "figure",
    "src": "/assets/learn/http-versions/en/figure-10-multiplexing-hol.svg",
    "alt": "HTTP/2 multiplexing and residual blocking caused by TCP loss",
    "caption": "Figure 10 - HTTP/2 multiplexing and residual blocking caused by TCP."
  },
  {
    "kind": "subhead",
    "text": "Classic symptom"
  },
  {
    "kind": "paragraph",
    "text": "A healthy TCP connection, responding HTTP/2 ping and some stopped streams may indicate an exhausted flow window or an application that has stopped consuming content."
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "HPACK: per-connection stateful field compression",
    "id": "hpack-per-connection-stateful-field-compression"
  },
  {
    "kind": "paragraph",
    "text": "APIs repeat fields such as method, schema, host, content type, and authentication. Sending them in full with each request increases overhead. HPACK compresses blocks of fields using a static table of common entries, a dynamic table constructed during connection, literal representations, and Huffman coding. A repeated entry can be referenced by index instead of relaying name and value."
  },
  {
    "kind": "paragraph",
    "text": "The pivot table is directional and synchronized by the order of the blocks in the connection. The encoder decides which values ​​to enter; the decoder maintains the same sequence. Changing the table size or receiving an invalid index may generate a compression error and terminate the connection. Intermediaries end HPACK: the gateway decodes client fields and creates its own encoding when connecting to the backend."
  },
  {
    "kind": "paragraph",
    "text": "Sensitive fields such as Authorization or cookies should not be indexed when doing so increases risk or retention. HPACK includes “never indexed” representation. Compression also interacts with side-channel attacks when secrets and controlled data share context. Field list size limits remain necessary, because a compact representation can expand to a lot of metadata."
  },
  {
    "kind": "figure",
    "src": "/assets/learn/http-versions/en/figure-11-hpack.svg",
    "alt": "Field blocks, static and dynamic tables, and compact HPACK representation",
    "caption": "Figure 11 - Conceptual components of HPACK."
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Negotiation: TLS, ALPN, h2 and h2c",
    "id": "negotiation-tls-alpn-h2-and-h2c"
  },
  {
    "kind": "paragraph",
    "text": "On the secure web, HTTP/2 is typically negotiated over TLS over ALPN. The client advertises protocols, for example `h2` and `http/1.1`; the server selects one and reports it in the handshake. This avoids sending a request in the wrong format. The certificate and name still need to be valid for the origin. Once h2 is selected, the client sends the preface and HTTP/2 frames."
  },
  {
    "kind": "paragraph",
    "text": "HTTP/2 can also operate without TLS using the h2c identifier, with prior knowledge or Upgrade from HTTP/1.1, but this mode is less common on public traffic. In internal networks, some components use h2c for gRPC, while TLS terminates in sidecar or gateway. This decision reduces encryption by one leap and needs to be evaluated according to trust, compliance and observability."
  },
  {
    "kind": "paragraph",
    "text": "Connection coalescing allows an HTTP/2 connection to be reused for multiple sources when resolution, certificate, and authority requirements are met. This improves efficiency, but can surprise connection-based balancing. Gateways and CDNs need to prevent unauthorized authority from being served on the connection. Logical origin remains determined by semantics, not just the connected IP address."
  },
  {
    "kind": "figure",
    "src": "/assets/learn/http-versions/en/figure-12-alpn.svg",
    "alt": "HTTP/2 or HTTP/1.1 negotiation over ALPN during TLS handshake",
    "caption": "Figure 12 - Negotiation of the application protocol with ALPN."
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "HTTP/3: HTTP semantics over QUIC",
    "id": "http-3-http-semantics-over-quic"
  },
  {
    "kind": "paragraph",
    "text": "HTTP/3 maps HTTP semantics to QUIC. QUIC is a connection-oriented transport encapsulated in UDP, with streams, flow control, loss detection, congestion control, security and path migration. TLS 1.3 is integrated into the handshake; There is no HTTP/3 mode without cryptographic protection as per the specification. The application sees individually ordered, reliable streams, not raw UDP datagrams."
  },
  {
    "kind": "paragraph",
    "text": "Just like HTTP/2, HTTP/3 uses HEADERS and DATA frames, but the frames travel in QUIC streams. Each request uses a bidirectional stream initiated by the client. Unidirectional streams carry control and QPACK. Functions that QUIC already offers, such as stream ID and flow control, are not duplicated by the HTTP layer. Therefore, some HTTP/2 frames and mechanisms disappear or change."
  },
  {
    "kind": "paragraph",
    "text": "Using UDP does not mean that HTTP/3 is unreliable. QUIC implements commit, retransmission, and stream ordering. The difference is that these functions are in user space, integrated with cryptography and capable of evolving without depending on the operating system's TCP implementation. Firewalls and middleboxes that block UDP can prevent QUIC, requiring a fallback to HTTP/2 or HTTP/1.1."
  },
  {
    "kind": "figure",
    "src": "/assets/learn/http-versions/en/figure-13-protocol-stacks.svg",
    "alt": "Comparison of HTTP/1.1, HTTP/2, and HTTP/3 stacks",
    "caption": "Figure 13 - Simplified stacks of HTTP/1.1, HTTP/2 and HTTP/3."
  },
  {
    "kind": "subhead",
    "text": "Mental model"
  },
  {
    "kind": "paragraph",
    "text": "HTTP/3 is not “HTTP/2 over UDP”. It reuses ideas from streams and frames, but delegates to QUIC properties that in HTTP/2 depend on separate TCP and TLS."
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "QUIC handshake, TLS 1.3 and 0-RTT",
    "id": "quic-handshake-tls-1-3-and-0-rtt"
  },
  {
    "kind": "paragraph",
    "text": "The QUIC handshake negotiates transport parameters and runs TLS 1.3. On a new connection, the client can send an Initial packet containing ClientHello data; the server responds and the keys evolve through encryption levels. Under normal conditions, application data can be sent with a low number of round trips. QUIC protects almost all control content, reducing the passive visibility available to middleboxes."
  },
  {
    "kind": "paragraph",
    "text": "On resumed connections, 0-RTT may allow the client to send application data before fully acknowledging the new handshake. This data does not have the same replay protection as 1-RTT data. Servers should accept 0-RTT only for replay-safe operations or apply anti-replay and idempotence mechanisms. A financial POST should not be considered secure just because it uses TLS."
  },
  {
    "kind": "paragraph",
    "text": "QUIC applies amplification protection before validating the client address: the server limits bytes sent relative to those received. Retry tokens can help validate origin and control abuse, at the cost of additional round tripping. Gateways at the edge need to scale handshake state, thresholds, and DoS protection without treating all UDP as sessionless traffic."
  },
  {
    "kind": "table",
    "caption": "Table 9 - Relevant steps of QUIC establishment.",
    "headers": [
      "Phase",
      "Protection/property",
      "Operational risk"
    ],
    "rows": [
      [
        "Initial",
        "Derivable keys to enable routing and handshake initiation.",
        "Not to be confused with lack of integrity; content is not strong secret against observer."
      ],
      [
        "Handshake",
        "TLS authenticates and negotiates final keys.",
        "Loss or blocking of UDP appears as failure before HTTP."
      ],
      [
        "1-RTT",
        "Normal data protected.",
        "Active streams and flow control."
      ],
      [
        "0-RTT",
        "Early data in resumption.",
        "Possible replay; restrict operations."
      ],
      [
        "Retry",
        "Additional address validation.",
        "Increases latency, reduces amplification and abusive state."
      ]
    ]
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "QUIC streams and head-of-line elimination between streams",
    "id": "quic-streams-and-head-of-line-elimination-between-streams"
  },
  {
    "kind": "paragraph",
    "text": "Each QUIC stream offers reliable, orderly delivery within the stream itself. Packets can carry frames from multiple streams. If a packet with data from stream B is lost, the receiver can continue delivering complete data from streams A and C. Only the missing point from B prevents orderly advancement of that stream. This eliminates the traversal blocking caused by single-stream TCP over HTTP/2."
  },
  {
    "kind": "paragraph",
    "text": "Improvement does not eliminate every blockage. A stream remains ordered; Losing early bytes prevents delivering later bytes from the same stream. Connection flow control can also block all streams if the receiver does not grant credit. QPACK dependencies can suspend decoding of a block of fields. Therefore, metrics and traces need to distinguish packet loss, stream blocked by data, stream blocked by fields and connection window."
  },
  {
    "kind": "paragraph",
    "text": "QUIC numbers data and acknowledges packets, but retransmits information in new packets, not “the same packet”. Packet numbers are never reused in the same space. Loss detection and congestion control were specified in associated documents. Implementation in user space allows evolution, but also requires specific observability of the library or proxy that terminates QUIC."
  },
  {
    "kind": "figure",
    "src": "/assets/learn/http-versions/en/figure-14-quic-loss-isolation.svg",
    "alt": "Loss isolation between streams of a QUIC connection",
    "caption": "Figure 14 - Loss isolation between QUIC streams."
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Connection IDs, NAT rebinding and path migration",
    "id": "connection-ids-nat-rebinding-and-path-migration"
  },
  {
    "kind": "paragraph",
    "text": "TCP identifies a connection by its set of addresses and ports. If a mobile device switches from Wi-Fi to cellular, the tuple changes and the connection is typically lost. QUIC uses Connection IDs chosen by endpoints, allowing you to associate packets from a new path to the existing connection. The new path is validated with challenges before being used fully."
  },
  {
    "kind": "paragraph",
    "text": "Connection IDs also allow balancers to forward packets without relying exclusively on the tuple. The design needs to consider privacy and prevent a stable identifier from allowing cross-network tracking; Endpoints can provide multiple IDs and retire them. QUIC-aware gateways and load balancers need to coordinate routing, retry/reset keys, and affinity."
  },
  {
    "kind": "paragraph",
    "text": "Migration is not a guarantee of continuity in any environment. Policies can disable it; firewalls can block the new path; the server may require validation; MTU and RTT changes alter performance. Logs must record migration and path validation to differentiate legitimate network exchange from instability or attack."
  },
  {
    "kind": "figure",
    "src": "/assets/learn/http-versions/en/figure-15-quic-migration.svg",
    "alt": "QUIC path migration with Connection IDs and new address validation",
    "caption": "Figure 15 - Path validation and migration in QUIC."
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Frames and control streams in HTTP/3",
    "id": "frames-and-control-streams-in-http-3"
  },
  {
    "kind": "paragraph",
    "text": "An HTTP/3 connection has one control stream per endpoint. It sends SETTINGS, GOAWAY and other control frames. Prematurely closing the control stream is a connection error. Requests use bidirectional streams from the client; each carries HEADERS, DATA, and trailers as permitted. Frames of a message cannot appear in any arbitrary order."
  },
  {
    "kind": "paragraph",
    "text": "As QUIC provides flow control and stream reset, HTTP/3 uses transport mechanisms for part of the lifecycle. Errors have HTTP/3 and QUIC codes. A cancellation may involve resetting the shipment and requesting to stop the opposite direction. Observability must associate QUIC stream, HTTP request and application correlation, as a single connection ID can carry many operations."
  },
  {
    "kind": "paragraph",
    "text": "HTTP/3 removes features from HTTP/2 that depended on the TCP stream, but preserves the concept of drainage. GOAWAY limits new accepted request streams. In deploy, the edge must announce closure, maintain the connection while allowed operations end, and direct new calls to another connection. Closing UDP state abruptly causes loss of all active operations."
  },
  {
    "kind": "table",
    "caption": "Table 10 - Basic structure of an HTTP/3 connection.",
    "headers": [
      "HTTP/3 element",
      "Transport",
      "Usage"
    ],
    "rows": [
      [
        "Request stream",
        "Customer-initiated bidirectional QUIC",
        "A request and its response."
      ],
      [
        "Control stream",
        "Unidirectional QUIC per endpoint",
        "SETTINGS, GOAWAY and HTTP control."
      ],
      [
        "QPACK encoder stream",
        "Unidirectional QUIC",
        "Pivot table updates."
      ],
      [
        "QPACK stream decoder",
        "Unidirectional QUIC",
        "Confirmations and cancellations."
      ],
      [
        "HEADERS",
        "Frame HTTP/3",
        "Starting pitches or trailers."
      ],
      [
        "DATE",
        "Frame HTTP/3",
        "Message content."
      ]
    ]
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "QPACK: field compression suitable for QUIC streams",
    "id": "qpack-field-compression-suitable-for-quic-streams"
  },
  {
    "kind": "paragraph",
    "text": "HPACK depends on the unique TCP connection order to synchronize the pivot table. In QUIC, streams can be delivered independently; a missed table update should not unnecessarily block all streams. QPACK uses unidirectional encoder and decoder streams to transmit instructions and acknowledgments, allowing blocks of fields in request streams to make controlled references."
  },
  {
    "kind": "paragraph",
    "text": "A block can depend on inputs not yet received and block until instructions arrive. The decoder announces limits for the number of blocked streams, and the encoder chooses between better compression and risk of blocking. Literal representations and static table allow you to avoid dependencies. As with HPACK, sensitive values ​​can be marked for non-indexing."
  },
  {
    "kind": "paragraph",
    "text": "Gateways terminate QPACK and create new context on next connection. A QPACK failure is a connection error and can affect many requests. Monitoring only HTTP status does not reveal this class of failure, because the message may not be reconstructed. HTTP/3 terminator logs must expose decompression error codes and blocked streams."
  },
  {
    "kind": "figure",
    "src": "/assets/learn/http-versions/en/figure-16-qpack.svg",
    "alt": "Encoder, decoder, requests and dynamic table flows in QPACK",
    "caption": "Figure 16 - QPACK conceptual flows."
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "HTTP/3, Alt-Svc, HTTPS/SVCB discovery and fallback",
    "id": "http-3-alt-svc-https-svcb-discovery-and-fallback"
  },
  {
    "kind": "paragraph",
    "text": "A client needs to know which origin offers HTTP/3 and on which endpoint. RFC 9114 defines advertisement by Alternative Services: an HTTP/1.1 or HTTP/2 response may include `Alt-Svc: h3=\":443\"`. The client stores the alternative for a limited time and tries QUIC. DNS can also provide HTTPS/SVCB records with protocol and endpoint parameters. Support varies between customers and infrastructure."
  },
  {
    "kind": "paragraph",
    "text": "The logical source remains the same, even if the alternative service is on a different host or port. The client needs to validate the authority and certificate according to the rules. Alt-Svc is not application redirection and does not change the displayed URI. If the QUIC attempt fails, clients typically continue or fall back to HTTP/2/1.1, but timing and failure cache details are implementation dependent."
  },
  {
    "kind": "paragraph",
    "text": "When troubleshooting, confirm that the client received an announcement, that UDP was attempted, that ALPN h3 was negotiated and that fallback occurred. A capture of just TCP traffic can show the call working and hide failed QUIC attempts. Blocking UDP/443 can increase initial latency even when fallback maintains availability. Incorrect HTTPS/SVCB records may direct modern clients to a different endpoint than the one used by legacy clients."
  },
  {
    "kind": "figure",
    "src": "/assets/learn/http-versions/en/figure-17-http3-discovery.svg",
    "alt": "Discovery, QUIC attempt, and fallback to HTTP/2 or HTTP/1.1",
    "caption": "Figure 17 - HTTP/3 discovery, attempt and fallback."
  },
  {
    "kind": "code",
    "text": "HTTP/1.1 200 OK\nAlt-Svc: h3=\":443\"; ma=86400\n# The client can attempt a QUIC connection to the same origin on port 443."
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Architectural comparison between HTTP/1.1, HTTP/2 and HTTP/3",
    "id": "architectural-comparison-between-http-1-1-http-2-and-http-3"
  },
  {
    "kind": "paragraph",
    "text": "Choosing a version is not just looking for “the newest one”. HTTP/1.1 has broad compatibility and simple observability, but requires more connections for concurrency and suffers from sequential blocking. HTTP/2 reduces overhead and allows many streams in one connection, being essential for gRPC, but it concentrates operations on a TCP and requires stream and flow metrics. HTTP/3 improves behavior under loss and mobility, but depends on UDP, ends encryption in the component that needs to interpret HTTP, and changes networking practices."
  },
  {
    "kind": "paragraph",
    "text": "Earnings depend on the profile. In a stable network and low latency, a small API may show modest difference. In lossy mobile connections, HTTP/3 can reduce impact between streams and preserve connection during path change. In an enterprise architecture with many intermediaries, the end-to-end benefit may be reduced if the edge receives H3 but the rest use H1.1 with saturated pools."
  },
  {
    "kind": "paragraph",
    "text": "Protocols also affect capacity. Ten thousand requests can use thousands of H1 connections, hundreds of H2 or H3 connections, depending on concurrency and limits. Balancing by connections is less representative in multiplexing. A single client can concentrate streams on one instance. Algorithms, limits and observability need to evolve with the protocol."
  },
  {
    "kind": "table",
    "caption": "Table 11 - Summary comparison of versions.",
    "headers": [
      "Appearance",
      "HTTP/1.1",
      "HTTP/2",
      "HTTP/3"
    ],
    "rows": [
      [
        "Transport",
        "TCP; Separate TLS over HTTPS",
        "TCP; typically TLS + ALPN",
        "QUIC over UDP; Integrated TLS 1.3"
      ],
      [
        "Format",
        "Lines and textual fields",
        "Binary frames",
        "Frames over QUIC streams"
      ],
      [
        "Competition",
        "Sequential/pipeline; multiple connections",
        "Multiplexed streams",
        "Loss-isolated multiplexed streams"
      ],
      [
        "Field compression",
        "Not standardized in the protocol",
        "HPACK",
        "QPACK"
      ],
      [
        "HOL",
        "On HTTP and TCP",
        "Eliminates HOL HTTP; keeps TCP HOL",
        "Loss blocks only affected stream"
      ],
      [
        "Network migration",
        "Connection often breaks",
        "Connection often breaks",
        "Connection IDs and path validation"
      ],
      [
        "Passive Observability",
        "Simpler after TLS termination",
        "Frames after TLS termination",
        "More encrypted control; requires QUIC terminator"
      ],
      [
        "Compatibility",
        "Maximum",
        "Broad on modern browsers/proxies",
        "Depends on UDP and edge support"
      ]
    ]
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "API Gateways and per-hop protocol translation",
    "id": "api-gateways-and-per-hop-protocol-translation"
  },
  {
    "kind": "paragraph",
    "text": "In an enterprise chain, each component can negotiate its own version. A client uses HTTP/3 with a CDN; CDN uses HTTP/2 with API Gateway; the gateway uses HTTP/1.1 with the backend. Semantics need to be preserved, but framing, compression, multiplexing and connection are reconstructed. There is no H3 stream “traversing” a gateway that ends HTTP."
  },
  {
    "kind": "paragraph",
    "text": "Translation changes behavior. Many multiplexed requests from an H2 connection may require multiple H1 connections to the backend or be serialized according to pooling. Trailers may be lost if the next protocol or product does not preserve them. A stream reset can turn into a socket cancellation, while the backend has already processed the operation. GOAWAY on one side has no direct correspondence in an independent connection on the other side."
  },
  {
    "kind": "paragraph",
    "text": "Policies must be semantic. Rate limiting per connection is inappropriate when a connection contains hundreds of streams. Payload limits need to consider uncompressed size. Cache must use correct authority and Vary. Observability needs to record inbound version, outbound version, reuse, stream ID when available, retry attempt and status created by the gateway."
  },
  {
    "kind": "figure",
    "src": "/assets/learn/http-versions/en/figure-18-protocol-per-hop.svg",
    "alt": "HTTP versions negotiated independently at each architecture hop",
    "caption": "Figure 18 - Independent HTTP versions at each hop of the architecture."
  },
  {
    "kind": "subhead",
    "text": "Architecture question"
  },
  {
    "kind": "paragraph",
    "text": "What is the version negotiated in each real connection: client-edge, edge-gateway, gateway-mesh and mesh-backend? The answer “the API uses HTTP/2” is usually incomplete."
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Using Axway API Gateway",
    "id": "using-axway-api-gateway"
  },
  {
    "kind": "paragraph",
    "text": "Axway API Gateway uses HTTP services to receive traffic and configurations from remote hosts for outgoing connections. In a real design, listener, interface, TLS, policy, routing and destination configurations participate in different hops. The investigation must separate the version and fields received by the listener from what is produced when connecting to the remote host. The documentation and installed version need to be consulted, as capabilities and standards may vary by release."
  },
  {
    "kind": "paragraph",
    "text": "When forwarding to a backend, persistent connection and pooling influence latency, distribution and socket consumption. Legacy configurations can use conservative HTTP behaviors, and teams must explicitly validate the desired outbound version. Transformations and filters can change content, fields, and streaming. Transaction Access Logs and policy tracing should record final status, connection time, response time, and routing error."
  },
  {
    "kind": "paragraph",
    "text": "A common scenario is that the client observes HTTP/2 at the edge, but the backend records HTTP/1.0 or HTTP/1.1. This is not necessarily an error; it could be a decision or default of the outbound hop. The risk arises when the application depends on trailers, streaming or competition that does not survive translation. Tests must cover the actual protocol, not just the public endpoint."
  },
  {
    "kind": "table",
    "caption": "Table 12 - Evidence for investigating HTTP on Axway API Gateway.",
    "headers": [
      "Checkpoint",
      "Evidence",
      "Question"
    ],
    "rows": [
      [
        "HTTP service/listener",
        "Port, TLS and protocol configuration",
        "What does the client negotiate with the gateway?"
      ],
      [
        "Policy flow",
        "Trace filters and transformations",
        "What fields and content have changed?"
      ],
      [
        "Routing/remote host",
        "Target, version, pool and timeout",
        "How does the gateway create the upstream connection?"
      ],
      [
        "Access log",
        "status, bytes, times, correlation",
        "Who produced the response and how long did it take?"
      ],
      [
        "Backend log",
        "protocol and authority observed",
        "What actually got into the service?"
      ]
    ]
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Application in Azure API Management",
    "id": "application-in-azure-api-management"
  },
  {
    "kind": "paragraph",
    "text": "In Azure API Management, the gateway receives calls, applies policies, and routes them to backends. Support for HTTP/2 inbound, outbound and gRPC depends on gateway type and generation, tier and configuration. The `forward-request` policy has an HTTP version attribute for supported scenarios. As the platform evolves, the official documentation must be treated as the source of truth for the instance used."
  },
  {
    "kind": "paragraph",
    "text": "The inbound version does not imply an outbound version. Current documentation describes behaviors where certain generations support HTTP/2 and can downgrade forwarding, while others allow you to configure HTTP/2 for the backend. This affects gRPC, trailers, and streaming. Tests should record `context`/traces, backend diagnostics, and instance metrics to confirm the effective path."
  },
  {
    "kind": "paragraph",
    "text": "Policies such as rewrite-uri, set-header, set-body, cache, retry and rate-limit operate on the logical message and can change observed characteristics. Retry needs to consider idempotence. Set-body generally implies access to content and may require buffering. Cache must respect identity and Vary. When combining Application Gateway, Front Door, APIM and backend, each service terminates its own connection and may have a different timeout."
  },
  {
    "kind": "table",
    "caption": "Table 13 - Points of attention in Azure API Management.",
    "headers": [
      "APIM Element",
      "HTTP Impact",
      "Validation"
    ],
    "rows": [
      [
        "Gateway Protocols",
        "Inbound and TLS capabilities",
        "Check configuration and tier."
      ],
      [
        "forward-request",
        "Version and timeout from jump to backend",
        "Confirm support of the gateway used."
      ],
      [
        "Retry policy",
        "Can repeat call after failure",
        "Classify method and idempotence."
      ],
      [
        "set-header/rewrite-uri",
        "Change forwarded message",
        "Compare trace with backend log."
      ],
      [
        "Cache policies",
        "Can respond without upstream",
        "Validate key, TTL and privacy."
      ],
      [
        "Diagnostics and telemetry",
        "Distinguish policy, gateway and backend",
        "Propagate correlation/trace context."
      ]
    ]
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Observability: what to measure in each version",
    "id": "observability-what-to-measure-in-each-version"
  },
  {
    "kind": "paragraph",
    "text": "Common metrics include request rate, status, latency, bytes, and errors. For HTTP/1.1, add open connections, new connections per second, reuse, pool wait, and resets. For HTTP/2, measure active streams per connection, concurrency threshold, RST_STREAM, GOAWAY, stream window, and compression errors. For HTTP/3, include QUIC retries, success/fallback, handshake, loss, RTT, migration, blocked streams, and QPACK errors."
  },
  {
    "kind": "paragraph",
    "text": "Latency must be decomposed. DNS, connection, TLS/QUIC, policy time, pool wait, time to first upstream byte and content transfer answer different questions. A high “total” metric does not identify cause. Distributed tracing starts when there is an HTTP message; Previous failures may require network logs and terminators. The gateway must reliably create or propagate correlation identifiers."
  },
  {
    "kind": "paragraph",
    "text": "Traffic captures remain useful, but encryption limits content. In TLS over TCP, the terminator can provide key logging in a controlled laboratory. QUIC encrypts more control elements, so qlog and library metrics are valuable. In banking production, collection must respect sensitive data: do not record tokens, cookies, PAN, payloads or complete headers without masking and legal basis."
  },
  {
    "kind": "table",
    "caption": "Table 14 - Specific observability by version.",
    "headers": [
      "Metric",
      "HTTP/1.1",
      "HTTP/2",
      "HTTP/3"
    ],
    "rows": [
      [
        "Competition Unit",
        "Connection/request",
        "Stream",
        "Stream QUIC"
      ],
      [
        "Coordinated closure",
        "Connection: close / FIN",
        "GOAWAY + streams",
        "GOAWAY + QUIC closure"
      ],
      [
        "Cancellation",
        "Closing connection may affect other calls",
        "RST STREAM",
        "Reset/stop-sending per stream"
      ],
      [
        "Field compression",
        "N/A at core",
        "HPACK errors",
        "QPACK errors and blocks"
      ],
      [
        "fallback signal",
        "New connection in another version",
        "ALPN http/1.1",
        "QUIC failure followed by H2/H1"
      ],
      [
        "Relevant loss",
        "TCP retransmission",
        "TCP retransmission affects streams",
        "Path/stream loss and QUIC recovery"
      ]
    ]
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "End-to-end HTTP troubleshooting method",
    "id": "end-to-end-http-troubleshooting-method"
  },
  {
    "kind": "paragraph",
    "text": "The first step is to determine whether there was a valid HTTP response. DNS, TCP, UDP, TLS, or QUIC errors may occur before any status. Tools sometimes convert failures into generic messages, but the gateway cannot have produced 504 if the call didn't even reach it. Identify the last component that has evidence of the attempt and the first that does not."
  },
  {
    "kind": "paragraph",
    "text": "When status exists, determine authorship. Compare public status, upstream status, internal error and executed policy. A 503 from the backend can be preserved; another 503 might be created by the gateway because no healthy endpoints were available. Headers like Server are not enough proof as they can be removed or faked. Use correlation and timestamps between controlled logs."
  },
  {
    "kind": "paragraph",
    "text": "Then record version and connection on each hop. Confirm ALPN, outbound protocol, reuse, stream reset, GOAWAY, retry and timeout. Compare method, authority, path, relevant fields, and length before and after the gateway. For intermittent failures, look for patterns by connection, instance, stream, payload size, version, network, and deploy time."
  },
  {
    "kind": "figure",
    "src": "/assets/learn/http-versions/en/figure-19-troubleshooting-tree.svg",
    "alt": "Initial tree to classify failures before and after an HTTP response",
    "caption": "Figure 19 - Initial tree to classify HTTP failures."
  },
  {
    "kind": "table",
    "caption": "Table 15 - Matrix of symptoms and evidence.",
    "headers": [
      "Symptom",
      "Priority hypotheses",
      "Evidence"
    ],
    "rows": [
      [
        "No response / connection error",
        "DNS, firewall, TCP/UDP, TLS/QUIC, port",
        "dig/nslookup, connect trace, handshake, edge logs."
      ],
      [
        "400 only via gateway",
        "Parsing, normalization, threshold, transformation",
        "Raw request in laboratory, policy trace, backend log."
      ],
      [
        "413/431",
        "Limit content or fields in some jump",
        "Settings and uncompressed actual size."
      ],
      [
        "502",
        "Upstream failure/invalidity, reset, protocol",
        "Internal gateway error, backend capture and log."
      ],
      [
        "503 flashing",
        "Pool, health, stream/connection limit, deploy",
        "Metrics per instance and connection."
      ],
      [
        "504",
        "Gateway-backend hop timeout or chain",
        "Latency breakdown and timeout budget."
      ],
      [
        "H2 works, H1 fails",
        "Framing, Trailers, Competition or Host",
        "Comparison of the message after translation."
      ],
      [
        "H3 slow before working",
        "QUIC attempt fails and fallback",
        "UDP/443, Alt-Svc/HTTPS RR and QUIC logs."
      ],
      [
        "RST in reuse",
        "Idle timeout misaligned",
        "Connection age and closure on the other hop."
      ],
      [
        "Duplicity after timeout",
        "Non-idempotent operation retry",
        "Attempt logs and idempotence key."
      ]
    ]
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Case Study 1: Duplicate POST after 504",
    "id": "case-study-1-duplicate-post-after-504"
  },
  {
    "kind": "paragraph",
    "text": "A client sends POST to create a transfer. The gateway forwards it to the backend, which confirms the transaction at the bank, but takes a while to generate the response. The gateway timeout expires and returns 504. The client interprets it as failure and repeats the call. Since POST is not idempotent by definition and there is no deduplication key, the backend creates a second transfer."
  },
  {
    "kind": "paragraph",
    "text": "The problem is not solved just by increasing timeout. The architecture must define idempotence semantics: client sends unique key; gateway preserves; backend records key and result atomically. On repeat, returns the previous result. The gateway can use retry only for failures clearly prior to submission or in classified operations. Logs need to show request ID, idempotency key, attempt and backend result."
  },
  {
    "kind": "subhead",
    "text": "Learning"
  },
  {
    "kind": "paragraph",
    "text": "Timeout means no confirmation to the observer, not proof of no effect on the remote system."
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Case Study 2: Public HTTP/2 and HTTP/1.1 backend saturation",
    "id": "case-study-2-public-http-2-and-http-1-1-backend-saturation"
  },
  {
    "kind": "paragraph",
    "text": "An edge receives thousands of HTTP/2 streams in a few connections and forwards them to the gateway. The gateway calls an HTTP/1.1 backend with a pool of 100 connections. During peak, streams enter quickly but wait for upstream connection. Inbound TCP metrics appear stable because there are few H2 connections; queue and latency grow in the outbound pool."
  },
  {
    "kind": "paragraph",
    "text": "The solution requires measuring competition by stream and wait pool, applying admission limits, adjusting backend capacity and considering HTTP/2 outbound when supported. Increasing pool without evaluating resources can shift overload to the service. Rate limiting per connection would be ineffective, as an H2 connection concentrates many streams. The goal is to align logical competition with actual upstream capacity."
  },
  {
    "kind": "subhead",
    "text": "Learning"
  },
  {
    "kind": "paragraph",
    "text": "In multiplexed protocols, connection is no longer a good approximation for the number of simultaneous operations."
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Case Study 3: HTTP/3 advertised but UDP blocked",
    "id": "case-study-3-http-3-advertised-but-udp-blocked"
  },
  {
    "kind": "paragraph",
    "text": "A CDN starts announcing `Alt-Svc: h3`. Modern clients try QUIC on UDP/443, but a corporate network blocks UDP. After a delay, the client falls back to HTTP/2 and the API works. Users report slowness only on first access; tests that force HTTP/2 do not reproduce. API logs show normal requests, because the attempt fails before the HTTP message arrives."
  },
  {
    "kind": "paragraph",
    "text": "The investigation needs to look at DNS, Alt-Svc, UDP packets, and fallback timing. The fix may involve enabling QUIC, removing advertising for specific networks when possible, or accepting fallback with monitoring. It should not be concluded that HTTP/3 caused an application error. The problem lies in the discovery and establishment of the transport, prior to the HTTP transaction observed by the gateway."
  },
  {
    "kind": "subhead",
    "text": "Learning"
  },
  {
    "kind": "paragraph",
    "text": "Fallback availability can hide a path failure and add invisible latency in application logs."
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Case study 4: H2 to H1 translation creates insecure framing",
    "id": "case-study-4-h2-to-h1-translation-creates-insecure-framing"
  },
  {
    "kind": "paragraph",
    "text": "A proxy accepts HTTP/2 and converts pseudo-fields and DATA to HTTP/1.1. A malformed combination passes H2 validation, but generates two incompatible length signals in H1. The backend interprets the message differently and controlled bytes make up the next request in the persistent connection. The vulnerability only appears when the path includes the specific translation."
  },
  {
    "kind": "paragraph",
    "text": "Mitigation requires validating the logical message before serializing H1, emitting exactly one framing mechanism, rejecting prohibited fields, updating intermediaries, and testing smuggling variations. Disabling reuse can reduce impact, but does not replace correction. WAF based only on H2 input text may not see the message produced to the backend."
  },
  {
    "kind": "subhead",
    "text": "Learning"
  },
  {
    "kind": "paragraph",
    "text": "The point of greatest risk is often the boundary between two parsers or versions, not an isolated parser."
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Practical reading and diagnostic labs",
    "id": "practical-reading-and-diagnostic-labs"
  },
  {
    "kind": "heading",
    "level": 3,
    "text": "Lab 1 - Compare HTTP/1.1 and HTTP/2 with cURL"
  },
  {
    "kind": "paragraph",
    "text": "Use a lab endpoint that supports both versions. Run calls forcing HTTP/1.1 and HTTP/2 and enable verbose output. Note resolution, connection, TLS, ALPN, request line displayed, reuse and timing. Repeat multiple calls in the same process to check reuse. Do not use productive endpoints or real tokens."
  },
  {
    "kind": "code",
    "text": "curl -v --http1.1 https://SEU-ENDPOINT-DE-LAB/status\ncurl -v --http2   https://SEU-ENDPOINT-DE-LAB/status\n# Also record times:\ncurl -sS -o /dev/null -w \"connect=%{time_connect} tls=%{time_appconnect} ttfb=%\n{time_starttransfer} total=%{time_total}\\n\" https://SEU-ENDPOINT-DE-LAB/status"
  },
  {
    "kind": "heading",
    "level": 3,
    "text": "Lab 2 - Inspect ALPN with OpenSSL"
  },
  {
    "kind": "paragraph",
    "text": "Connect to a lab server and advertise h2 and HTTP/1.1. Confirm the selected protocol. Then force just http/1.1 and compare. The goal is to realize that ALPN occurs in the TLS handshake before HTTP messages. In proxy environments, run from different network points to identify different endpoints."
  },
  {
    "kind": "code",
    "text": "openssl s_client -connect SEU-ENDPOINT-DE-LAB:443 -servername SEU-ENDPOINT-DE-LAB -alpn\n\"h2,http/1.1\"\n# Search for: ALPN protocol: h2"
  },
  {
    "kind": "heading",
    "level": 3,
    "text": "Lab 3 - Cache and revalidation"
  },
  {
    "kind": "paragraph",
    "text": "Create a lab service that returns ETag and Cache-Control. Do a GET, store the ETag and send If-None-Match. Confirm 304 no content. Modify the resource and repeat. Add a controlled cache proxy and watch Age and Vary. Document how authentication changes the possibility of shared cache."
  },
  {
    "kind": "code",
    "text": "curl -i https://SEU-ENDPOINT-DE-LAB/resource\ncurl -i -H \"If-None-Match: \\\"ETAG-RECEBIDA\\\"\" https://SEU-ENDPOINT-DE-LAB/resource"
  },
  {
    "kind": "heading",
    "level": 3,
    "text": "Lab 4 - Framing HTTP/1.1 in an isolated environment"
  },
  {
    "kind": "paragraph",
    "text": "On a local server created for study, send messages with correct Content-Length and valid chunked. Notice how the server reads the body. Then test invalid inputs only in an authorized laboratory and confirm that they are rejected, without trying to exploit third-party systems. The goal is to understand parsing and validation, not to execute attacks."
  },
  {
    "kind": "code",
    "text": "printf \"POST /echo HTTP/1.1\\r\\nHost: localhost\\r\\nContent-Length: 5\\r\\n\\r\\nhello\" | nc\n127.0.0.1 8080"
  },
  {
    "kind": "heading",
    "level": 3,
    "text": "Lab 5 - Observe HTTP/3 and fallback"
  },
  {
    "kind": "paragraph",
    "text": "Use a lab tool and endpoint with known HTTP/3 support. Record QUIC attempt, final protocol, and fallback when UDP is blocked in a test environment. Compare time to first byte. Do not change corporate firewalls without authorization. On clients that support qlog, generate a local trace and view handshake and streams."
  },
  {
    "kind": "code",
    "text": "curl -v --http3 https://SEU-ENDPOINT-DE-LAB/status\ncurl -v --http3-only https://SEU-ENDPOINT-DE-LAB/status\n# The availability of options depends on the compilation of cURL and the QUIC library."
  },
  {
    "kind": "heading",
    "level": 3,
    "text": "Lab 6 - Mapping versions by hop in the gateway"
  },
  {
    "kind": "paragraph",
    "text": "Publish a lab API that returns protocol, host, and correlation headers observed by the backend. Call the public endpoint in different versions and compare listener logs, policy traces, and backend output. Draw each independent connection and note TLS, ALPN, version, pool, timeout and transformation. This map is more useful than declaring a single “API version”."
  },
  {
    "kind": "list",
    "ordered": true,
    "items": [
      "Register client-edge protocol.",
      "Register edge-gateway protocol.",
      "Register gateway-backend protocol.",
      "Compare Host/:authority and Forwarded fields.",
      "Execute call with large content and observe buffering.",
      "Run controlled deploy and observe draining/GOAWAY or resets."
    ]
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "HTTP Architecture Checklist for Enterprise APIs",
    "id": "http-architecture-checklist-for-enterprise-apis"
  },
  {
    "kind": "table",
    "caption": "Table 16 - Design and review checklist.",
    "headers": [
      "Theme",
      "Review Questions"
    ],
    "rows": [
      [
        "Semantics",
        "Do methods and statuses follow their properties? Do retries respect idempotence?"
      ],
      [
        "URI/authority",
        "Are host and original authority validated and preserved when necessary?"
      ],
      [
        "Fields",
        "Which ones are removed, created, signed, or trusted at each hop?"
      ],
      [
        "Content",
        "Is there transformation, compression, buffering, streaming or uncompressed limit?"
      ],
      [
        "Cache",
        "What responses can be stored? Does the key consider identity and Vary?"
      ],
      [
        "HTTP/1.1",
        "Is framing strict? Are pools and idle timeouts coordinated?"
      ],
      [
        "HTTP/2",
        "What stream, field and window limits are configured? Is GOAWAY treated?"
      ],
      [
        "HTTP/3",
        "Is UDP allowed? Is there fallback? How are qlog and QUIC metrics collected?"
      ],
      [
        "Gateway",
        "Have inbound/outbound versions been confirmed by evidence?"
      ],
      [
        "Observability",
        "Are public status, upstream status and internal error separate?"
      ],
      [
        "Security",
        "Have protocol translations been tested against ambiguities and smuggling?"
      ],
      [
        "Capacity",
        "Is competition measured by requests/streams, not just connections?"
      ]
    ]
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Chapter Summary",
    "id": "chapter-summary"
  },
  {
    "kind": "paragraph",
    "text": "HTTP provides stable semantics for interaction between clients, servers and intermediaries. Methods express intention; statuses communicate results; fields describe control and representation; cache and conditions allow for reuse and concurrency. These concepts are independent of JSON, REST, or the transport version."
  },
  {
    "kind": "paragraph",
    "text": "HTTP/1.1 encodes textual messages into a TCP stream and requires strict framing rules. Persistent connections save handshakes, but pooling, timeouts, and divergent parsing create risks. HTTP/2 introduces frames, streams, multiplexing, flow control and HPACK, reducing overhead and head-of-line at the HTTP level, although TCP loss still affects all streams."
  },
  {
    "kind": "paragraph",
    "text": "HTTP/3 uses QUIC over UDP, integrates TLS 1.3, isolates loss per stream, supports Connection IDs and uses QPACK. Discovery can occur via Alt-Svc and HTTPS/SVCB records, with fallback to previous versions. In gateways, each hop negotiates its own version; the professional needs to observe the logical message and connection of each segment."
  },
  {
    "kind": "paragraph",
    "text": "Reliable troubleshooting starts by classifying whether the failure occurred before or after there was an HTTP response, identifying who created the status and mapping versions, timeouts, pools, streams and transformations. The architecture must protect semantics, idempotence, and security during translations between protocols."
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Review exercises",
    "id": "review-exercises"
  },
  {
    "kind": "list",
    "ordered": true,
    "items": [
      "Explain the difference between HTTP semantics and specific version mapping.",
      "Why doesn't stateless mean that an application cannot maintain a session?",
      "Differentiate resource, representation, content and framing."
    ]
  },
  {
    "kind": "list",
    "ordered": true,
    "items": [
      "Which components of a URI participate in an HTTP request and which are not sent?",
      "Differentiate secure method from idempotent method and give examples.",
      "Why repeating POST after timeout can produce duplicity even when the client received 504?",
      "Who can create a 502 on a chain with multiple proxies?",
      "Why should reason phrase not be used in client logic?",
      "Differentiate Content-Type, Content-Encoding and Transfer-Encoding.",
      "Explain why Vary is necessary in caching content negotiation.",
      "What does `Cache-Control: no-cache` mean?",
      "How do ETag and If-Match help avoid lost update?",
      "Why doesn't TCP preserve HTTP message boundaries?",
      "What risks exist when Content-Length and Transfer-Encoding are interpreted differently?",
      "Why can a persistent connection only be reused after completely consuming the previous message?",
      "Describe head-of-line in HTTP/1 pipelining.",
      "",
      "How does HTTP/2 map a request to frames and streams?",
      "Differentiate between HTTP/2 flow control and TCP congestion control.",
      "What is the role of SETTINGS_MAX_CONCURRENT_STREAMS?",
      "How does GOAWAY help with deployments and draining?",
      "How does HPACK reduce overhead and why does it create per-connection state?",
      "What is the role of ALPN in HTTP/2 negotiation?",
      "Why is HTTP/2 still subject to TCP head-of-line?",
      "Why is stating that HTTP/3 is “UDP untrusted” wrong?",
      "How does QUIC enable path migration?",
      "What is the risk of 0-RTT for effect trades?",
      "How does QPACK differ conceptually from HPACK?",
      "How can Alt-Svc advertise HTTP/3 without changing the logical origin?",
      "Why can an API use HTTP/3 publicly and HTTP/1.1 in the backend?",
      "What additional metrics are needed in HTTP/2 and HTTP/3?",
      "Propose a timeout budget for client, edge, gateway and backend.",
      "Design a secure retry strategy for payment GET and POST.",
      "Explain how an H2 to H1 translation can create a risk of request smuggling.",
      "Describe the evidence needed to prove which version was used in each jump."
    ]
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Architectural Discussion Questions",
    "id": "architectural-discussion-questions"
  },
  {
    "kind": "list",
    "ordered": false,
    "items": [
      "In an organization with thousands of APIs, what criteria justify enabling HTTP/2 or HTTP/3 at the edge and backend?",
      "How should balancers distribute load when few connections carry many streams?",
      "Which fields should be removed or rebuilt in a chain with CDN, WAF, gateway and service mesh?",
      "How to guarantee idempotence of financial operations when timeouts and retries can occur in several components?",
      "What level of QUIC observability is acceptable in a regulated environment without exposing sensitive data?",
      "When is buffering at the gateway necessary and when does it destroy streaming benefits?",
      "How to test parsing and protocol translation security on an automated conveyor belt?"
    ]
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Glossary",
    "id": "glossary"
  },
  {
    "kind": "table",
    "caption": "Table 17 - Chapter glossary.",
    "headers": [
      "Term",
      "Definition"
    ],
    "rows": [
      [
        "ALPN",
        "TLS extension used to negotiate the application protocol, such as h2 or http/1.1."
      ],
      [
        "Alt-Svc",
        "Mechanism for advertising alternative HTTP service, including HTTP/3 endpoint."
      ],
      [
        "Authority",
        "Component that identifies the host and logical port of the source."
      ],
      [
        "Shared cache",
        "Cache that can reuse responses for more than one user or client."
      ],
      [
        "Connection coalescing",
        "Controlled reuse of an HTTP/2 connection to more than one origin."
      ],
      [
        "Connection ID",
        "QUIC identifier that allows you to associate packets with the connection beyond the IP/port tuple."
      ],
      [
        "Content",
        "Message data sequence after appropriate framing decodes."
      ],
      [
        "Content negotiation",
        "Selection of representation according to preferences and capabilities."
      ],
      [
        "Control stream",
        "Unidirectional HTTP/3 stream that carries connection control."
      ],
      [
        "End-to-end field",
        "Field whose meaning applies to final endpoints, even through intermediaries."
      ],
      [
        "ETag",
        "Opaque validator associated with a representation."
      ],
      [
        "Flow control",
        "Mechanism that prevents the sender from exceeding buffers announced by the receiver."
      ],
      [
        "Framing",
        "Rules for determining limits and content of messages in transport."
      ],
      [
        "GOAWAY",
        "Signal that a connection will not accept new streams/requests beyond a certain limit."
      ],
      [
        "HEADERS frame",
        "Frame that transports compressed block of fields in HTTP/2 or HTTP/3."
      ],
      [
        "Head-of-line blocking",
        "Delay in independent operations caused by a missing or slow previous item."
      ],
      [
        "HPACK",
        "Field compression used by HTTP/2."
      ],
      [
        "HTTP/3",
        "Mapping HTTP semantics to QUIC."
      ],
      [
        "Idempotence",
        "Property by which repeating the same intention produces the same intended effect."
      ],
      [
        "Intermediate",
        "HTTP participant that receives and forwards messages, such as a proxy or gateway."
      ],
      [
        "Multiplexing",
        "Sharing a connection across multiple independent exchanges."
      ],
      [
        "Origin",
        "Logical source responsible for the resource identified by the URI."
      ],
      [
        "Pseudo-header",
        "Special HTTP/2/3 field starting with a colon, such as :method."
      ],
      [
        "QPACK",
        "Field compression used by HTTP/3."
      ],
      [
        "QUIC",
        "Secure, multiplexed transport over UDP used by HTTP/3."
      ],
      [
        "Representation",
        "Data and metadata that represents the state of a resource."
      ],
      [
        "Request smuggling",
        "Attack based on divergence on request limits between intermediaries."
      ],
      [
        "Safe method",
        "Method whose intention is essentially reading."
      ],
      [
        "Stream",
        "Logical and ordered channel within a multiplexed connection."
      ],
      [
        "Trailer",
        "Field sent after the message content."
      ],
      [
        "Validator",
        "Metadata used to test whether the stored representation remains valid."
      ],
      [
        "Vary",
        "Field that informs dimensions of the request used to select a response."
      ]
    ]
  },
  {
    "kind": "heading",
    "level": 2,
    "text": "Official references and recommended reading",
    "id": "official-references-and-recommended-reading"
  },
  {
    "kind": "paragraph",
    "text": "The references below prioritize specifications and official documentation. RFCs must be consulted with their errata and any documents that update them. Product documentation must be validated for the version, tier and gateway used in the environment."
  },
  {
    "kind": "list",
    "ordered": true,
    "items": [
      "RFC 9110 - HTTP Semantics - https://www.rfc-editor.org/rfc/rfc9110.html",
      "RFC 9111 - HTTP Caching - https://www.rfc-editor.org/rfc/rfc9111.html",
      "RFC 9112 - HTTP/1.1 - https://www.rfc-editor.org/rfc/rfc9112.html",
      "RFC 9113 - HTTP/2 - https://www.rfc-editor.org/rfc/rfc9113.html",
      "RFC 9114 - HTTP/3 - https://www.rfc-editor.org/rfc/rfc9114.html",
      "RFC 7541 - HPACK: Header Compression for HTTP/2 - https://www.rfc-editor.org/rfc/rfc7541.html",
      "RFC 9204 - QPACK: Field Compression for HTTP/3 - https://www.rfc-editor.org/rfc/rfc9204.html",
      "RFC 9000 - QUIC: A UDP-Based Multiplexed and Secure Transport - https://www.rfc-editor.org/rfc/rfc9000.html",
      "RFC 9001 - Using TLS to Secure QUIC - https://www.rfc-editor.org/rfc/rfc9001.html",
      "RFC 9002 - QUIC Loss Detection and Congestion Control - https://www.rfc-editor.org/rfc/rfc9002.html",
      "RFC 7838 - HTTP Alternative Services - https://www.rfc-editor.org/rfc/rfc7838.html",
      "RFC 9460 - Service Binding and HTTPS DNS Resource Records - https://www.rfc-editor.org/rfc/rfc9460.html",
      "RFC 9651 - Structured Field Values for HTTP - https://www.rfc-editor.org/rfc/rfc9651.html",
      "RFC 7301 - TLS Application-Layer Protocol Negotiation - https://www.rfc-editor.org/rfc/rfc7301.html",
      "RFC 9308 - Applicability of the QUIC Transport Protocol - https://www.rfc-editor.org/rfc/rfc9308.html",
      "RFC 9312 - Manageability of the QUIC Transport Protocol - https://www.rfc-editor.org/rfc/rfc9312.html",
      "IANA HTTP Field Name Registry - https://www.iana.org/assignments/http-fields/http-fields.xhtml",
      "IANA HTTP Status Code Registry - https://www.iana.org/assignments/http-status-codes/http-status-codes.xhtml",
      "Axway - Configure HTTP services - https://docs.axway.com/bundle/axway-open-docs/page/docs/apim_policydev/apigw_gw_instances/general_services/index.html",
      "Axway - Configure remote host settings - https://docs.axway.com/bundle/axway-open-docs/page/docs/apim_policydev/apigw_gw_instances/general_remote_hosts/index.html",
      "Microsoft - API gateway in Azure API Management - https://learn.microsoft.com/en-us/azure/api-management/api-management-gateways-overview",
      "Microsoft - forward-request policy - https://learn.microsoft.com/en-us/azure/api-management/forward-request-policy",
      "Microsoft - Manage protocols and ciphers in API Management - https://learn.microsoft.com/en-us/azure/api-management/api-management-howto-manage-protocols-ciphers"
    ]
  },
  {
    "kind": "heading",
    "level": 3,
    "text": "Suggested reading order"
  },
  {
    "kind": "list",
    "ordered": true,
    "items": [
      "Read the introductory sections and terminology of RFC 9110 before studying specific versions.",
      "Study RFC 9112 with a focus on message framing, connection and parsing security.",
      "Read the overview, frames, streams, flow control and errors of RFC 9113; consult HPACK in parallel.",
      "Study QUIC in the connection, streams, flow control and migration sections of RFC 9000, followed by TLS integration in RFC 9001.",
      "Read RFC 9114 and QPACK, relating what was delegated to QUIC.",
      "Consult Alt-Svc and HTTPS/SVCB to understand discovery and fallback.",
      "Finally, map the capabilities and limitations of the version of Axway and Azure APIM used in the work."
    ]
  },
  {
    "kind": "subhead",
    "text": "Closing"
  },
  {
    "kind": "paragraph",
    "text": "Mastering HTTP means mastering the boundary between business intent and transport. The next chapter will delve deeper into HTTPS and TLS, explaining how identity, confidentiality, and integrity protect each of these connections."
  }
];
